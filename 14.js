'use strict';

console.log("--Pujan Karki--12'D'--");

const p = 100000;
const t = 2;
const r = 10;

const si = (p * t * r) / 100;

console.log(
  `The simple interest for Rs.${p} for ${t} years at ${r}% rate is ${si}`
);
